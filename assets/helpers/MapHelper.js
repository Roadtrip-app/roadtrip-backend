export const dist = (point1, point2, miles = true) => {

	const p = 0.017453292519943295;  // Math.PI / 180, converts degrees to radians
	return Math.acos(Math.sin(lat1) * Math.sin(lat2))
}

export const calcMidpoint = (point1, point2) => {

}